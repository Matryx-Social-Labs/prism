import { FrontPage } from "@/components/FrontPage";

// `/` is the chart (founder decision D5, 2026-09-15). No marketing hero: the
// day's list is the pitch, and a first visitor gets one promise line above it.
export default function Page() {
  return <FrontPage />;
}
