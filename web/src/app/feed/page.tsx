import { FrontPage } from "@/components/FrontPage";

// Same chart as `/` — every existing link, redirect and tab that says /feed
// still lands on today's list.
export default function FeedPage() {
  return <FrontPage />;
}
